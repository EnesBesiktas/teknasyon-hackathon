import React from 'react';
import { MobilePreview } from '../components/ui/layout/MobilePreview';

export const Billing: React.FC = () => {
    return (
        <div className="min-h-screen bg-gradient-to-br from-gray-900 via-gray-800 to-black">
            <MobilePreview />
        </div>
    );
};
